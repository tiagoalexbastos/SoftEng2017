Deploy project with Docker
==========================


Docker Compose
-----------------

In our project we decided to use a Docker Compose because it's easier to make the automatic deploy and create the network layer links between the containers.


**We use the following containers:**

Elastic Search

Logstash

Kibana

Tomcat(JSF)

Alpine-java(JPA)

Alpine-python(Alarms)